name: lint and jest tests

on: push

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - name: make lint
        run: make lint
      - name: make lint
        run: npx jest